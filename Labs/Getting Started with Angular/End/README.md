This lab uses the Angular CLI to create the project so no files will be shown here. The following steps were run:

1. npm install -g @angular/cli
2. ng --version
3. ng new hello-world
4. ng build
5. ng serve -o